# Copy to .env.local and fill in your key (never commit .env.local)
# Restart `npm run dev` or `npm run preview` after changes.

OPENAI_API_KEY=

# Optional — e.g. gpt-4o-mini, gpt-4o, gpt-4-turbo
# OPENAI_MODEL=gpt-4o-mini
