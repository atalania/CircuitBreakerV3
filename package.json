{
  "name": "circuit-game-ver2",
  "private": true,
  "type": "module",
  "dependencies": {
    "stem-assistant-bridge": "file:vendor/stem-assistant-bridge"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
